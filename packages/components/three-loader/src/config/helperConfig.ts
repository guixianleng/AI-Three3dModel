import type { IHelperOptions } from '../types/scene'

/**
 * 默认辅助工具配置
 */
export const defaultHelperConfig: IHelperOptions = {
  showGrid: true,
  showStats: true,
  showAxes: true,
  showFloor: true,
  gridSize: 2000,
  gridDivisions: 100,
  gridColor: 0x888888,
  axesSize: 1000,
  floorColor: 0xcccccc
} 